# NextWork-Set-Up-a-Web-App-in-the-Cloud
This project demonstrates how to set up a cloud-based development environment on AWS using an EC2 instance. I configured IAM access, installed Java (Corretto 8) and Maven, and generated a Java web app using Maven.  I developed and edited the project using both the terminal and VS Code Remote - SSH, then tested it in a live cloud environment.
